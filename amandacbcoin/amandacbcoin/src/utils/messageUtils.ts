import { WAMessage } from '@whiskeysockets/baileys'; export function extractMessageContent(message: WAMessage): string | null { if (message.message?.conversation) { return message.message.conversation; } if (message.message?.extendedTextMessage?.text) { return message.message.extendedTextMessage.text; } return null; } export function extractUserName(message: WAMessage): string { return message.pushName || 'Usuário'; }
